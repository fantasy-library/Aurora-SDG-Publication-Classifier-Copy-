streamlit>=1.37
python-dotenv>=1.0.1
pandas>=2.2
requests>=2.31
truststore>=0.9  # HTTPS: use OS certificate store (fixes SSL on Windows / MITM proxies)
altair>=5.2
scholarly>=1.7.11
httpx<0.28.0  # Temporary fix for compatibility issues with scholarly proxy setup
free-proxy==1.0.6  # Scholarly FreeProxies expects older API
networkx>=3.0
plotly>=5.22
tomli>=2.0.1; python_version < "3.11"
